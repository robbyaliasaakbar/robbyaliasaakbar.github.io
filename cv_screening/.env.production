# BUILD LIVE (GitHub Pages) — dibaca Vite pas `npm run build` (mode production).
# Isinya URL PUBLIK (funnel), bukan rahasia — aman ikut ke-push.
# Dev lokal (`npm run dev`) TIDAK baca file ini.
VITE_N8N_URL=https://aispec.tail06293c.ts.net:10000/webhook/upload-cv
VITE_AUTH_URL=https://aispec.tail06293c.ts.net
# Tanpa VITE_BACKEND_PORT: app ini manggil n8n (di atas) + auth (di atas), tidak ada backend data Node.
VITE_AUTH_PORT=7002
VITE_FRONTEND_PORT=7008
